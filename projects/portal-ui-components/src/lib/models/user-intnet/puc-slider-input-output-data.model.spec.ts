import { PucSliderInputOutputData } from './puc-slider-input-output-data.model';

describe('PucSliderInputOutputData', () => {
  it('should create an instance', () => {
    expect(new PucSliderInputOutputData()).toBeTruthy();
  });
});
