import { AsyncLoaderDirective } from './async-loader.directive';

describe('AsyncLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
